import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * SettingsButton - Basically the same as the Image Class, however, I had to make another class due to issues with
 * the paint order.
 * 
 * @author Andrew Qiao
 * @version June 2021
 */
public class SettingsButton extends Image
{
    /**
     * Constructor for SettingsButton Class, calls its superclass.
     * 
     * @param image             GreenfootImage for the image that should be displayed.
     */
    public SettingsButton(GreenfootImage image)
    {
        super(image);
    }
}
